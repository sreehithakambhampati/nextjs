//This is part of private folder(starts with _) you cannot view it in the browser
export default function privatFolder()
{
    return <h1>Nothing</h1>
}
// If we really want an underscore we can use %5F